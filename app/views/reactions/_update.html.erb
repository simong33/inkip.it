
<%= form_for(current_user.reactions.find_by("chapter_id = ?", @chapter.id), remote: true, html: {method: :patch}) do |f| %>
  <%= button_tag(type: 'submit', class: "inkdrops", id: "inkdrop") do %>
    <%= hidden_field_tag :chapter, @chapter.id %>
    <%= render "svg/inkdrop" %>
    <span class="ink-count" id="ink-count"></span>
    <div class="inkdrops-value" id="ink-total-count"></div>
  <% end %>
<% end %>
