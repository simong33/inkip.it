<div class="content-dashboard wrapper__signin">
  <div class="container container__signin">

    <h2>Inscription</h2>

<!--     <%- if devise_mapping.omniauthable? %>
      <%- resource_class.omniauth_providers.each do |provider| %>
        <%= link_to omniauth_authorize_path(resource_name, provider), class: "facebook-cta" do %>
          <i class="fa fa-facebook" aria-hidden="true"></i><%= "Se connecter avec #{OmniAuth::Utils.camelize(provider)}" %>
        <% end %>
      <% end -%>
    <% end -%>

    <p>ou avec votre email</p> -->

    <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
      <%= f.error_notification %>

      <div class="form-inputs">
        <%= f.input :user_name, label: "Nom d'utilisateur", required: true, autofocus: true %>
        <%= f.input :email, required: true, autofocus: true %>
        <%= f.input :password, label: 'Mot de passe', required: true, hint: ("#{@minimum_password_length} caractères minimum" if @minimum_password_length) %>
        <%= f.input :password_confirmation, label: 'Confirmation du mot de passe', required: true %>
      </div>

      <div class="form-actions">
        <%= f.button :submit, "Inscription" %>
      </div>
    <% end %>

    <%= render "devise/shared/links" %>
  </div>
</div>
