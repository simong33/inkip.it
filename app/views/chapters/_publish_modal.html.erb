<div class="modal fade" id="publishChapterModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">Publier votre chapitre</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <p>Etes-vous certain de vouloir publier votre chapitre ? Vous pourrez le retirer à tout moment.</p>
        <%= form_for [@chapter.book, @chapter] do |f| %>
          <%= f.hidden_field :published, value: true %>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Fermer</button>

          <%= button_tag(type: 'submit', class: "btn btn-primary btn-submit") do %>
            Publier
          <% end %>
        <% end %>
      </div>
    </div>
  </div>
</div>
